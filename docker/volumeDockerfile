###################################################################
# Dockerfile to build a client and server and have a shared volume
# between client and server
# Based on ubuntu
###################################################################

# Set the base image to Ubuntu
FROM ubuntu

# File Author
MAINTAINER karthik

# Create a directory
RUN mkdir /data

# Copy the file into the directory
ADD strings.txt /data/strings.txt

# Expose the data volume container
VOLUME /data
